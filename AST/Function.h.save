#ifndef FUNCTION_H
#define FUNCTION_H

#include <list>
#include <Expression.h>
#include <GlobalContext.h>

class Function : Expression
{
    public:
        Function(std::string body);
        virtual ~Function();
        std::string getName();
        void setName(std::string name);
        void setArgsNames(list<std::string> *args);
        Number *eval(list<std::string> *args);
        Number* getArg(std::string name);

    protected:
    private:
        std::string m_name;
        std::list<std::string> *m_argsNames;
};

#endif // FUNCTION_H
