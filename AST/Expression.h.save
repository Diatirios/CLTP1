#ifndef EXPRESSION_H
#define EXPRESSION_H

#include <string>
class Nu

class Expression
{
    public:
        /** Default constructor */
        Expression(std::string body);
        /** Default destructor */
        virtual ~Expression();
        virtual ::Number* eval() = 0;

        std::string getBody();

    protected:
        void setBody(std::string body);
    private:
        std::string m_body;
};

#endif // EXPRESSION_H
